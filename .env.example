# Database Configuration
# Replace with your actual PostgreSQL connection string
DATABASE_URL="postgresql://username:password@localhost:5432/nexus_db?schema=public"

# NextAuth Configuration
# Generate a secure secret with: openssl rand -base64 32
NEXTAUTH_SECRET="your-nextauth-secret-here"

# Optional: NextAuth URL (automatically detected in development)
# NEXTAUTH_URL="http://localhost:3000"

# Node Environment
NODE_ENV="development"

# Optional: Server-side dev bypass (NOT RECOMMENDED for production)
# Temporarily bypasses authentication middleware for debugging
# SERVER_DEV_BYPASS="true"

# Optional: Other configuration
# Add any other environment variables your application needs here
