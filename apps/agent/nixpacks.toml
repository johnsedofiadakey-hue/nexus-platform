# Nixpacks configuration for Railway - Agent App
[phases.setup]
nixPkgs = ["nodejs_20", "pnpm"]

[phases.install]
cmds = [
  "pnpm install --frozen-lockfile"
]

[phases.build]
cmds = [
  "pnpm run db:generate",
  "cd apps/agent && pnpm build"
]

[start]
cmd = "cd apps/agent && pnpm start"
